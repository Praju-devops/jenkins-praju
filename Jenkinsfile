pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "okkk prajuu is Compiling the java source code"
                sh "java --version"
                sh 'python hello-world.py'
            }
        }

    }
}