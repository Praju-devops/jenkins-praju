pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "okkk my praju is Compiling the java source code"
                sh "java --version"
                sh 'python hello-world.py'
            }
        }

    }
}